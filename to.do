(need to be able to change things like: "use base qw(Capability);"
 to "use base qw(Org::FRDCSA::Capability)"
 )

(
 Implement the 

 process > file && tail -f file

 if `cat file` =~ /unrepresentable/
 clean, debianize and rebuild

 record if build failed using same method

 implement better system for debug and live
 )